instances:
  - url: insurgateway-680184.docs.buildwithfern.com
    custom-domain: docs.insurgateway.com

title: InsurGateway

colors:
  accentPrimary:
    dark: "#6366f1"
    light: "#4f46e5"
  background:
    dark: "#0f172a"
    light: "#ffffff"

navigation:
    layout:
      - api: API Reference
        flattened: true
        layout:
          - section: Authentication
            contents:
              - POST /api/Authentication/login
              - POST /api/Authentication/RefreshToken
          - section: Offline
            contents:
              - GET /api/offline/jobs

logo:
  dark: logo.png
  light: logo.png
favicon: favicon.png
